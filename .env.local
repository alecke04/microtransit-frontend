# Backend Endpoints (when backend is running, comment out NEXT_PUBLIC_USE_MOCK_DATA to test real calls)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
NEXT_PUBLIC_API_KEY=TEAM_GPS_01_KEY

# Mock Data Mode (set to "true" to use fake streaming data instead of backend)
NEXT_PUBLIC_USE_MOCK_DATA=false
